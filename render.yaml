services:
  - type: web
    name: half-lips
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "waitress-serve --port $PORT api.index:app"
    envVars:
      - key: FLASK_ENV
        value: production
